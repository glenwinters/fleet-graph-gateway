{
  "name": "fleet-graph-gateway",
  "version": "0.0.1",
  "description": "Fleet component: GraphQL supergraph gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@apollo/gateway": "^2.0.0-preview.12",
    "apollo-server": "^3.6.7",
    "dotenv": "^16.0.0",
    "graphql": "^16.3.0"
  }
}
